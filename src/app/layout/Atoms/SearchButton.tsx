import { FiSearch } from "react-icons/fi"

const SearchButton = () => {
  return (
    <button className="w-full h-full flexCenter inset-0 hover:text-lightRed transition-3 ">
      <FiSearch />
    </button>
  )
}

export default SearchButton
